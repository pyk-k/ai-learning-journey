input永远返回字符串（str）

try/except/else 各自执行时机

try是可能抛异常的那段代码

except是检测到try输入有问题才使用

else是try没问题时使用

今天暴露的不好习惯就是不细心对重复出现的错误没有修改，写完不主动运行测试

&#x20;

\## Day 2 复盘 · 2026-08-29



\### 完成

\- 单词统计器：字符串小写归一、isalpha 清洗标点、split 切词、字典计数、sorted 按值降序



\### 核心认知

1\. `input()` 返回字符串；`.lower()` 返回新字符串，必须赋值接住

2\. 字典计数要先判断 `if word in counts:`，避免 KeyError

3\. `sorted(data, key=lambda x: x\[1], reverse=True)` 按字典值降序排列

4\. Python 代码规范：=、+、, 两边要空格；循环变量用单数



\### 今天犯的错误

\- `==` 与 `=` 混淆

\- `counts\["word"]` 误把变量写成字符串字面量

\- 标点清洗缺失

\- 多次提问前没有先运行代码验证

\- git 冲突：同一文件在本地和远程改了不同版本




\- Day 3：函数封装 + 文件读取，把单词统计器拆成函数并支持读取 txt 文件
迭代器中map和fillter的区别，map主要是执行如upper，然后fillter主要是进行筛选选出满足函数条件的，且两者输出都要转换形式如list（）
format函数format（1，'20'）1是数字型默认右对齐向右移动20个单位，如果是hello则左对齐不会移动
#权限访问
如果在函数中定义属性用age=1全局都可已调用，_age=1表示受保护但子类也可以调用，__age=1表示私有只有在类里面可以调用，
如__age在class ss中stu=ss(),不可以直接stu.__age但可以stu._ss__age,定义类也是如此

类的深拷贝与浅拷贝
前提：**拷贝针对可变对象（list、dict、自定义类实例）**，不可变对象（int、str、元组）拷贝行为特殊。
模块：`import copy`
- **浅拷贝 `copy.copy()`**：只复制**最外层对象**，里面嵌套的子对象**共用同一份内存**
- **深拷贝 `copy.deepcopy()`**：**递归复制所有层级**，里面嵌套的子对象全部新建独立副本，完全互不影响
浅拷贝
1. 创建**新的 Person 实例**
2. 把原对象所有属性的值**直接赋值**给新对象（只是拷贝引用地址，不会拷贝里面嵌套可变对象
3. hobbies = ["读书","跑步"]
p1 = Person("kk", hobbies)

# 浅拷贝
p2 = copy.copy(p1)

print(p1 is p2) # False：p1、p2是两个不同实例（外层新建）
print(p1.hobby is p2.hobby) # True！！hobby列表是同一个，共用

# 修改嵌套列表
p1.hobby.append("游戏")
print(p2.hobby) # ['读书', '跑步', '游戏']
# p2的爱好跟着变！因为hobby是同一个列表

深拷贝
全部递归新建，每一层都独立
用copy.deepcopy()
p2拷贝p1他们两不仅是不同的实例列表页不是同一个相互改变不会影响


day4
异步基础学习
1. 开始烧水，**不用原地等着**，转身去泡方便面
2. 泡面在泡的时候，去刷短视频
3. 哪个事情先完成，就回去处理哪个

> 
> 重点：**不是同一时间同时干两件事（不是分身），而是等待的时候，不发呆，去做别的活。**
> 异步本质：**等待 IO 的时候，把空闲时间利用起来**。
> IO = 需要等待的事情：网络请求、读文件，就像烧水，需要等，你本人不用一直动手。
> ## 对应代码里的角色

- **事件循环 = 你这个人**，负责盯着所有任务，看哪个任务等待完了
- **协程 = 烧水、泡面、刷视频，一件一件小事**
- `async def`：标记这件事可以中途停下来，交给循环去干别的
- `await`：就是【我要开始等待了！你（事件循环）先去处理别的任务，等好了叫我回来】
> 
> await 只能放在 async 函数里面，就像：**只有这件事本身允许中途暂停，你才能停下来去干别的**
- `Task`：一次性把好几个任务交给事件循环，让它帮忙调度
1. `async def` → 协程函数；调用它得到协程对象，**不会自动运行**
2. `asyncio.run(xxx)`：启动事件循环，入口，只能在最外层写，执行任务。async定义的只能用asyncio.run调用
3. `await xxx`：暂停当前协程，让出控制权；只能写在 async 函数内部
4. `asyncio.create_task()`：创建后台任务，实现并发；gather 是批量并发
